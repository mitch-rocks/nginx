version: "3.8"

services:
  nginx:
    image: nginx:1.31-alpine

    deploy:
      mode: global
      restart_policy:
        condition: any

    ports:
      - target: 80
        published: 80
        protocol: tcp
        mode: host

    networks:
      - global

networks:
  global:
    external: true